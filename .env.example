# Environment Variables Template
# Copy this file to .env and replace with your actual values
# IMPORTANT: Never commit the .env file to version control

NODE_ENV=production
SESSION_SECRET=your-unique-session-secret-here-generate-a-strong-random-string
GITHUB_CLIENT_ID=your-github-oauth-client-id-here
GITHUB_CLIENT_SECRET=your-github-oauth-client-secret-here
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
ADMIN_PASSWORD=your-secure-admin-password-here

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with your actual credentials
# 3. For GitHub OAuth: Create an app at https://github.com/settings/applications/new
# 4. For MongoDB: Create a cluster at https://cloud.mongodb.com/
# 5. Generate a strong SESSION_SECRET (32+ characters)
# 6. Set a secure ADMIN_PASSWORD
# 7. For production deployment, add these variables to your hosting platform (e.g., Vercel environment variables)
